// const mongoose = require('mongoose');

// // Define Transaction Schema
// const TransactionSchema = new mongoose.Schema({
//   mobileNumber: { type: String, required: true }, // Removed `unique` constraint
//   customerName: { type: String, required: true },
//   amount: { type: Number, required: true },
//   remarks: { type: String, default: '' }, // Default value for optional remarks
//   createdAt: { type: Date, default: Date.now }, // Automatically add creation date
// });

// // Export Transaction Model
// module.exports = mongoose.model('Transaction', TransactionSchema);
